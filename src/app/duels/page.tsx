import { PhoneScreen } from "@/components/PhoneScreen";

export default function DuelsPage() {
  return (
    <PhoneScreen title="Duels" nav>
      <p className="text-sm text-(--color-text-muted)">
        Dieser Bereich (Gegner wählen, Wette setzen, Match, Ergebnis) folgt im nächsten
        Bauabschnitt.
      </p>
    </PhoneScreen>
  );
}
